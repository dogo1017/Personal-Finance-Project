Check if user has all libraries installed
    - If any not installed than give them the option to do automatically or give them instructions on how ot do it manually, not letting them continue till it is installed correctly

Show login menu with text boxes for password and username or buttons to enter information or sign up instead 
    - If information is entered then check if hashed passowrd mathes saved username in csv
        - if not then tell them their password is incorrect and go back to login
    - If sign in selected show the menu to enter their username and password
        - if username already in use gie error, if passwrod doesn tmeet requiremetnes give error
        - if return selected to back to login
        - if sign in is completed save password and username to csv, go to login screen

